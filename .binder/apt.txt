# GDAL libraries and command-line tools
gdal-bin
libgdal-dev

# PROJ (coordinate reference transformations)
proj-bin
proj-data
libproj-dev

# GEOS (geometry engine used by Shapely)
geos-bin
libgeos-dev

# Spatial index libraries (recommended for GeoPandas)
libspatialindex-dev

# Optional but recommended scientific libraries
libhdf5-dev
libnetcdf-dev
