# GDAL runtime libs (safe)
gdal-bin

# PROJ runtime
proj-bin
proj-data

# GEOS (for Shapely)
geos-bin
libgeos-dev

# RTree / spatial index
libspatialindex-dev

# Scientific libs (runtime only)
libhdf5-dev
libnetcdf-dev
